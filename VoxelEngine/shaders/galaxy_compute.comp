#version 450

layout(local_size_x = 10, local_size_y = 1, local_size_z = 1) in;

struct Star {
    vec2 position;
    float size;
};

layout(std430, binding = 0) buffer StarBuffer {
    Star stars[];
};

void main() {
    uint gid = gl_GlobalInvocationID.x;
    if (gid < stars.length()) {
        // In this simple example, we're not modifying the stars.
        // Update positions or sizes here.
    }
}
